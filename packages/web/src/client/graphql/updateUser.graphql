mutation UpdateUser(
  $userId: String!
  $email: String
  $hashedPassword: String
  $salt: String
) {
  updateUser(
    userId: $userId
    email: $email
    hashedPassword: $hashedPassword
    salt: $salt
  ) {
    userId
  }
}
