/*
  ids and types specified here:
  - cannot be created in the 'new document' menu
  - cannot be duplicated, unpublished or deleted
*/
export const LOCKED_DOCUMENT_IDS = []

export const LOCKED_DOCUMENT_TYPES = ['pairerProfile']

// References to include in 'internal' links

export const PAGE_REFERENCES = [{ type: 'pairerProfile' }]

export const PREVIEWABLE_DOCUMENT_TYPES = []

// Root-level slugs which cannot be created by regular pages
export const RESERVED_SLUGS = []
